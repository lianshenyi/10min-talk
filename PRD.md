# PRD — 拾分钟知识卡

## 1. 背景与目标

### 1.1 背景
碎片时间适合主动学习，但“选题—搜索—表达—复盘”常被选题困难和缺少节奏打断。本产品用随机专业词条和明确计时，把一次学习压缩成可重复的小闭环。

### 1.2 目标
交付一个**本地优先、无需账号**的响应式网页：随机抽取跨领域专业词条，进行 10 分钟研究，再用 3 或 5 分钟口述输出，并获得 AI 对理解、表达与应用的反馈。

### 1.3 非目标 (Out of Scope)
- 不提供账号、多人协作、云同步或排行榜。
- 不上传录音到 AI；不提供服务端密钥托管。
- 不把 AI 评价作为权威事实判断。
- MVP 不制作原生 App，也不承诺后台或锁屏持续录音。

## 2. 目标用户

| 角色 | 场景 | 痛点 |
|---|---|---|
| 主动学习者 | 每天抽出约 15 分钟拓宽知识面 | 不知道学什么，收藏多但输出少 |
| 知识工作者 | 会前、通勤或休息时训练表达 | 缺少结构化的短时练习和反馈 |

## 3. 核心功能 (MVP)

### F1. 专业随机抽卡
- 首批覆盖 12 个领域、144 个精选词条，后续扩展到 240+。
- 支持拖拽或按钮换卡；最近 20 次不重复。
- 在线搜索到的百科候选由用户确认后才加入个人词库。

### F2. 十分钟研究
- 固定 10 分钟，可暂停、恢复或提前结束。
- 提供必应、维基百科和 Google Scholar 搜索入口。
- 刷新、切换标签页或设备唤醒后按截止时间恢复。
- 研究阶段可选启用 AI 研究速览（与 F4 共用配置）；速览以 SSE 流式渲染，部分模型（如 MiniMax）只输出思考块时回退为中文思考草稿，避免 UI 卡顿。实现细节见 `spec.md` §5.4。

### F3. 三至五分钟口述
- 开始前选择 3 或 5 分钟，3 秒准备后录音。
- 支持提前停止、回放、重录；根据浏览器能力生成实时转写。
- 转写不可用时可手动编辑或粘贴文字，不阻塞流程。

### F4. AI 自动评价
- 支持 OpenAI-compatible 与 Anthropic Messages 两种完整 Endpoint。
- 从理解、表达、应用三个维度评分，给出纠错、亮点和下一步问题。
- 接口失败不丢失录音与稿件，可重试或跳过。

### F5. 本地历史
- 练习、录音、转写和评价保存在当前浏览器。
- 支持回放、查看、删除和导出；浏览器可能清理站点数据的限制需明确提示。

## 4. 非功能需求

| 维度 | 要求 |
|---|---|
| 性能 | 首屏本地开发环境 2 秒内可交互；卡片手势保持流畅 |
| 平台兼容 | 当前 Chrome、Edge、Safari；Firefox 降级为录音加手动文字 |
| 隐私/安全 | 录音不上传；API Key 加密后存 localStorage；无远程埋点和第三方脚本 |
| 离线/网络 | 内置抽卡、计时、录音、历史离线可用；在线词条和 AI 可降级 |
| 无障碍 | 键盘可完成主流程，支持减少动态效果和静音 |

## 5. 用户体验流程

1. 打开网页，选择 3 或 5 分钟输出时长。
2. 滑动卡片或点击“换一张”，确认感兴趣的词条。
3. 启动 10 分钟研究，通过搜索入口自行查资料。
4. 进入准备倒计时，授权麦克风并开始口述。
5. 回放录音、校对转写稿，提交 AI 评价或稍后处理。
6. 查看反馈并保存本次记录，回到牌组开始下一次。

## 6. 成功指标

- [ ] 连续 20 次抽卡不重复，12 个领域均可被抽到。
- [ ] 页面后台停留、刷新后，倒计时误差不超过 1 秒。
- [ ] 麦克风拒绝、转写不支持、AI 失败时仍能完成并保存练习。
- [ ] localStorage 中不存在明文 API Key，AI 请求不含音频数据。
- [ ] 手机与桌面均可完成抽卡、研究、录音和复盘闭环。

## 7. 风险与依赖

| 风险 | 缓解 |
|---|---|
| 浏览器语音识别能力不一致 | 能力探测，录音与识别解耦，提供手动文本兜底 |
| 自定义 AI Endpoint 被 CORS 阻止 | 设置页真实请求测试，按鉴权/跨域/超时分类提示 |
| 浏览器存储被清理或配额不足 | 捕获写入错误，提供导出，不宣称永久保存 |
| 前 long-term API Key 暴露于前端运行时 | 本地加密、无第三方脚本、风险提示；未来可增加本地代理 |

